import classes from './AppSideControler.module.scss'

const AppSideControler = () => {
    return (
        <div className={`${classes['side-controler']}`}>

        </div>
    )
}

export default AppSideControler